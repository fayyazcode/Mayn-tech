node_modules
.next
out
.DS_Store

# Secrets. Never commit these.
# NOTE: because they are ignored here, a Git-based deployment will NOT carry them
# to the server. Set the same variables in your hosting panel instead.
.env
.env.local
.env.production
